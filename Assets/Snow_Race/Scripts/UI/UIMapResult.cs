using System.Collections.Generic;
using UnityEngine;

public class UIMapResult : MonoBehaviour
{
    // [SerializeField] private RecordItem recordItem;
    // [SerializeField] private RectTransform spawnPos;
    //
    // private List<MapObjects> _mapObjects => CreateMapData.Instance.mapObjects;
    // private void OnEnable()
    // {
    //     for (int i = 0; i < _mapObjects.Count; i++)
    //     {
    //         var record = Instantiate(recordItem, spawnPos);
    //         record.InitData(_mapObjects[i].id, GameManager.Instance.mapRecord[i]);
    //     }
    // }
}
